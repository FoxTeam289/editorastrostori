<div class="popup" data-popup>
  <div class="popup__wrap">
    <div class="popup__info">
      <p>Ваша карта готова.</p>
      <p>Осталось <b>добавить в корзину</b> и оплатить заказ</p>
    </div>
    <button class="popup__btn" type="button" data-add-to-bag>
      Добавить в корзину
    </button>
  </div>
</div>
<style lang="scss">
  @import "../styles/settings";

  .popup {
    position: fixed;
    top: 0;
    left: 0;
    z-index: 999;
    width: 100%;
    height: 100%;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
    opacity: 0;
    visibility: hidden;
    pointer-events: none;
    transition: all .3s ease 0s;
    background-color: rgba(255, 255, 255, 0.3);

    &.active {
      opacity: 1;
      visibility: visible;
      pointer-events: auto;
    }

    @include des {
      -webkit-backdrop-filter: blur(d(10));
      backdrop-filter: blur(d(10));
    }

    @include mob {
      padding: m(50) 0;
      -webkit-backdrop-filter: blur(m(10));
      backdrop-filter: blur(m(10));
    }

    &__wrap {
      position: relative;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;

      background-color: var(--white);
      border-style: solid;
      border-color: var(--gray);

      @include des {
        width: d(470);
        padding: d(20) d(10) d(30);
        border-radius: d(25);
        border-width: d(1);
        grid-gap: d(30);
        min-height: d(220);
      }

      @include mob {
        width: m(280);
        padding: m(20) m(16);
        border-radius: m(20);
        border-width: m(1);
        grid-gap: m(20);
        min-height: m(159);

        & br {
          display: none;
        }
      }
    }

    
    &__info {
      width: 100%;
      display: flex;
      flex-direction: column;
      align-items: center;
      text-align: center;

      @include des {
        grid-gap: d(10);
      }

      @include mob  {
        grid-gap: m(6);
      }

      & p {
        width: 100%;
        font-weight: 500;

        &:first-of-type {
          line-height: 130%;

          @include des {
            font-size: d(18);
            letter-spacing: d(-0.54);
          }

          @include mob {
            font-size: m(15);
            letter-spacing: m(-0.45);
          }
        }

        &:last-of-type {
          color: var(--gray-dark);

          @include des {
            font-size: d(15);
            letter-spacing: d(-0.45);
          }

          @include mob {
            font-size: m(12);
            letter-spacing: m(-0.36);
          }
        }

        & b {
          color: var(--black);
          font-weight: inherit;
        }
      }
    }

    &__btn {
     width: auto;
      border-style: solid;
      border-color: var(--orange);

      display: -webkit-box;
      display: -ms-flexbox;
      display: flex;
      -webkit-box-align: center;
      -ms-flex-align: center;
      align-items: center;
      -webkit-box-pack: center;
      -ms-flex-pack: center;
      justify-content: center;

      color: var(--orange);
      background-color: var(--pink);
      font-weight: 500;

      transition: transform 0.15s ease 0s;


      @include des {
        border-width: d(1);
        height: d(40);
        border-radius: d(50);
        padding: 0 d(20);
        font-size:d(15);
        letter-spacing:d(-0.3);
      }

      @include mob {
        height: m(34);
        border-radius: m(50);
        border-width: m(1);
        padding: 0 m(12) m(1);
        font-size: m(11);
        font-size:m(14);
        letter-spacing:m(-0.14);
      }

      @media (hover: hover) {
        transition-duration: .3s;
        cursor: pointer;

        &:hover {
          transform: scale(1.05);
        }
      }

      &:active {
        transform: scale(0.98);
      }
    }
  }
</style>
