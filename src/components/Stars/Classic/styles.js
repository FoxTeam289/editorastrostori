/** @format */

import { motion } from "framer-motion";
import styled from "styled-components";
import { des, mob } from "../../../styles/media";
import { d, m } from "../../../styles/convert";

export const ClassicCom = styled(motion.svg)`
  width: auto;
  height: 100%;
`;
